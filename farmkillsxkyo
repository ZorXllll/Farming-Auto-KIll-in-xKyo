local PlaceID = game.PlaceId
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Cache tracking variables
local totalKills = 0
local sessionKills = 0
local maxHopTime = 80
local serverTimeLeft = maxHopTime
local FastPunchConnection = nil

-- --- NEON RED SCREEN GUI CREATION ---
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HopKillTrackerPro"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local successGui = pcall(function()
    ScreenGui.Parent = CoreGui
end)
if not successGui then
    pcall(function()
        ScreenGui.Parent = player:WaitForChild("PlayerGui")
    end)
end

-- Main Window Frame (Expanded to fit progress bar)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 300, 0, 205)
MainFrame.Position = UDim2.new(0, 40, 0, 40)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 12, 12)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(255, 0, 51)
MainStroke.Thickness = 2
MainStroke.Transparency = 0.1
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
MainStroke.Parent = MainFrame

local MainGradient = Instance.new("UIGradient")
MainGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(45, 0, 12)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(12, 10, 10))
})
MainGradient.Rotation = 45
MainGradient.Parent = MainFrame

-- Top Bar Header
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundTransparency = 1
TopBar.Parent = MainFrame

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Size = UDim2.new(1, -20, 1, 0)
TitleLabel.Position = UDim2.new(0, 14, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Farming Kills || ZorX~Private"
TitleLabel.TextColor3 = Color3.fromRGB(255, 40, 80)
TitleLabel.TextSize = 20
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TopBar

local TitleGlow = Instance.new("UIStroke")
TitleGlow.Color = Color3.fromRGB(255, 0, 60)
TitleGlow.Thickness = 1
TitleGlow.Transparency = 0.5
TitleGlow.Parent = TitleLabel

local ToggleButton = Instance.new("TextButton")
ToggleButton.Name = "ToggleButton"
ToggleButton.Size = UDim2.new(0, 24, 0, 24)
ToggleButton.Position = UDim2.new(1, -32, 0.5, -12)
ToggleButton.BackgroundColor3 = Color3.fromRGB(35, 15, 20)
ToggleButton.Text = "-"
ToggleButton.TextColor3 = Color3.fromRGB(255, 100, 120)
ToggleButton.TextSize = 17
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.Parent = TopBar

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 6)
ToggleCorner.Parent = ToggleButton

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Color = Color3.fromRGB(255, 0, 60)
ToggleStroke.Thickness = 1
ToggleStroke.Transparency = 0.3
ToggleStroke.Parent = ToggleButton

local Divider = Instance.new("Frame")
Divider.Name = "Divider"
Divider.Size = UDim2.new(1, -28, 0, 1)
Divider.Position = UDim2.new(0, 14, 0, 40)
Divider.BackgroundColor3 = Color3.fromRGB(60, 20, 30)
Divider.BorderSizePixel = 0
Divider.Parent = MainFrame

local ContentContainer = Instance.new("Frame")
ContentContainer.Name = "Content"
ContentContainer.Size = UDim2.new(1, -28, 1, -55)
ContentContainer.Position = UDim2.new(0, 14, 0, 48)
ContentContainer.BackgroundTransparency = 1
ContentContainer.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 6)
UIListLayout.Parent = ContentContainer

local function CreateStatCard(name, labelText, order)
    local Card = Instance.new("Frame")
    Card.Name = name
    Card.Size = UDim2.new(1, 0, 0, 28)
    Card.BackgroundColor3 = Color3.fromRGB(22, 12, 15)
    Card.BorderSizePixel = 0
    Card.LayoutOrder = order
    Card.Parent = ContentContainer

    local CardCorner = Instance.new("UICorner")
    CardCorner.CornerRadius = UDim.new(0, 6)
    CardCorner.Parent = Card

    local CardStroke = Instance.new("UIStroke")
    CardStroke.Color = Color3.fromRGB(255, 0, 60)
    CardStroke.Thickness = 1
    CardStroke.Transparency = 0.6
    CardStroke.Parent = Card

    local Label = Instance.new("TextLabel")
    Label.Name = "Value"
    Label.Size = UDim2.new(1, -12, 1, 0)
    Label.Position = UDim2.new(0, 6, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Text = labelText
    Label.TextColor3 = Color3.fromRGB(255, 230, 235)
    Label.TextSize = 17
    Label.Font = Enum.Font.GothamMedium
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Parent = Card

    return Label
end

local KillsLabel = CreateStatCard("KillsCard", "Total Kills: 0", 1)
local GainedLabel = CreateStatCard("GainedCard", "Gained Kills: 0", 2)
local TimerLabel = CreateStatCard("TimerCard", "Next Hop in: 60s", 3)

-- --- PROGRESS BAR CREATION ---
local ProgressContainer = Instance.new("Frame")
ProgressContainer.Name = "ProgressContainer"
ProgressContainer.Size = UDim2.new(1, 0, 0, 12)
ProgressContainer.BackgroundColor3 = Color3.fromRGB(22, 12, 15)
ProgressContainer.BorderSizePixel = 0
ProgressContainer.LayoutOrder = 4
ProgressContainer.Parent = ContentContainer

local ProgressCorner = Instance.new("UICorner")
ProgressCorner.CornerRadius = UDim.new(0, 6)
ProgressCorner.Parent = ProgressContainer

local ProgressStroke = Instance.new("UIStroke")
ProgressStroke.Color = Color3.fromRGB(255, 0, 60)
ProgressStroke.Thickness = 1
ProgressStroke.Transparency = 0.6
ProgressStroke.Parent = ProgressContainer

local ProgressBar = Instance.new("Frame")
ProgressBar.Name = "ProgressBar"
ProgressBar.Size = UDim2.new(1, 0, 1, 0)
ProgressBar.BackgroundColor3 = Color3.fromRGB(255, 40, 80)
ProgressBar.BorderSizePixel = 0
ProgressBar.Parent = ProgressContainer

local BarCorner = Instance.new("UICorner")
BarCorner.CornerRadius = UDim.new(0, 6)
BarCorner.Parent = ProgressBar

local minimized = false
ToggleButton.MouseButton1Click:Connect(function()
    minimized = not minimized
    ContentContainer.Visible = not minimized
    ToggleButton.Text = minimized and "+" or "-"
    TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quad), {
        Size = minimized and UDim2.new(0, 300, 0, 40) or UDim2.new(0, 300, 0, 205)
    }):Play()
end)

local function UpdateDisplay()
    KillsLabel.Text = "Total Kills: " .. totalKills
    GainedLabel.Text = "Gained Kills: " .. sessionKills
    TimerLabel.Text = "Next Hop in: " .. math.max(0, serverTimeLeft) .. "s"
    
    local percentage = math.clamp(serverTimeLeft / maxHopTime, 0, 1)
    TweenService:Create(ProgressBar, TweenInfo.new(0.9, Enum.EasingStyle.Linear), {
        Size = UDim2.new(percentage, 0, 1, 0)
    }):Play()
end

local function setupLeaderboardTracking()
    local leaderstats = player:WaitForChild("leaderstats", 15)
    if not leaderstats then return end

    local killsValue = leaderstats:FindFirstChild("Kills") or leaderstats:FindFirstChild("Kills ")

    if killsValue and (killsValue:IsA("IntValue") or killsValue:IsA("NumberValue")) then
        local lastKills = killsValue.Value
        totalKills = lastKills
        
        killsValue.Changed:Connect(function(newVal)
            local diff = newVal - lastKills
            if diff > 0 then
                sessionKills = sessionKills + diff
            end
            lastKills = newVal
            totalKills = newVal
            UpdateDisplay()
        end)
    end
    
    UpdateDisplay()
end
task.spawn(setupLeaderboardTracking)

-- Server Hopper Logic
local function GETOUT()
    local Services = setmetatable({}, {
       __index = function(self, name)
          local success, cache = pcall(function()
             return cloneref(game:GetService(name))
          end)
          if success then
             rawset(self, name, cache)
             return cache
          else
             error("Invalid Service: " .. tostring(name))
          end
       end
    })
    local JobId = game.JobId
    local servers = {}
    local req = game:HttpGet("https://games.roblox.com/v1/games/" .. PlaceID .. "/servers/Public?sortOrder=Desc&limit=100&excludeFullGames=true")
    local body = Services.HttpService:JSONDecode(req)

    if body and body.data then
       for i, v in next, body.data do
          if type(v) == "table" and tonumber(v.playing) and tonumber(v.maxPlayers) and v.playing < v.maxPlayers and v.id ~= JobId then
             table.insert(servers, 1, v.id)
          end
       end
    end
    if #servers >= 1 then
        Services.TeleportService:TeleportToPlaceInstance(PlaceID, servers[math.random(1, #servers)], game.Players.LocalPlayer)
    end
end

-- Server hops automatically after 80 seconds (with live progress bar countdown)
task.spawn(function()
    while serverTimeLeft > 0 do
        task.wait(1)
        serverTimeLeft = serverTimeLeft - 1
        UpdateDisplay()
    end
    GETOUT()
end)

-- Auto-Kill & Fast Punch Integration Logic
_G.autokillall = true
_G.punchanim = true

FastPunchConnection = RunService.Heartbeat:Connect(function()
    if _G.punchanim then
        local Character = player.Character
        if Character then
            local Humanoid = Character:FindFirstChildOfClass('Humanoid')
            local PunchTool = player.Backpack:FindFirstChild('Punch') or Character:FindFirstChild('Punch')
            
            if PunchTool then
                local attackTime = PunchTool:FindFirstChild('attackTime')
                if attackTime then 
                    attackTime.Value = 0 
                end
                
                if Humanoid and PunchTool.Parent ~= Character then 
                    Humanoid:EquipTool(PunchTool) 
                end
                
                PunchTool:Activate()
            end
            
            local remotes = ReplicatedStorage:FindFirstChild("RemotesEvent")
            if remotes and remotes:FindFirstChild("SizeChanged") then
                remotes.SizeChanged:FireServer(1)
            end
        end
    end
end)

local function AutoKillLobby()
    local targetList = Players:GetPlayers()
    
    for _, targetPlayer in ipairs(targetList) do
        -- Skip targeting friends (uses Roblox's built-in IsFriendsWith method)
        local isFriend = false
        pcall(function()
            isFriend = player:IsFriendsWith(targetPlayer.UserId)
        end)

        if targetPlayer ~= player and not isFriend and targetPlayer.Character then
            local tRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            local tHumanoid = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
            
            if tRoot and tHumanoid and tHumanoid.Health > 0 then
                local startTime = tick()
                while tick() - startTime < 0.4 and tHumanoid.Health > 0 and targetPlayer.Character and targetPlayer.Character.Parent do
                    local myChar = player.Character
                    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    local myHumanoid = myChar and myChar:FindFirstChildOfClass("Humanoid")
                    
                    if myChar and myRoot and myHumanoid then
                        myRoot.CFrame = tRoot.CFrame * CFrame.new(0, 0, 0)
                        
                        local tool = myChar:FindFirstChild("Punch") or player.Backpack:FindFirstChild("Punch")
                        if tool then
                            if tool.Parent ~= myChar then
                                myHumanoid:EquipTool(tool)
                            end
                            tool:Activate()
                        end
                        
                        local remotes = ReplicatedStorage:FindFirstChild("RemotesEvent")
                        if remotes and remotes:FindFirstChild("SizeChanged") then
                            remotes.SizeChanged:FireServer(1)
                        end
                    end
                    RunService.Heartbeat:Wait()
                end
            end
        end
    end
end

task.spawn(function()
    while true do
        if _G.autokillall and player.Character and player.Character:FindFirstChildOfClass("Humanoid") and player.Character:FindFirstChildOfClass("Humanoid").Health > 0 then
            AutoKillLobby()
        end
        RunService.Heartbeat:Wait() 
    end
end)
